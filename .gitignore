# --------------------------------------------------------------------
# 1. 사용자 지정 제외 항목
# --------------------------------------------------------------------
# 데이터 및 모델 파일이 포함된 폴더 제외
data/

# .으로 시작하는 모든 파일 및 폴더 제외 (설정 파일, 숨김 폴더 등)
.*
kaggle.json
rclone

# [예외] .gitignore 파일 자체는 깃허브에 올라가야 관리가 되므로 예외 처리
!.gitignore

# --------------------------------------------------------------------
# 2. Python & 시스템 기본 제외 항목
# --------------------------------------------------------------------
# 컴파일된 파이썬 파일 (필요 없음)
__pycache__/
*.py[cod]
*$py.class

# 가상환경 폴더 (로컬 환경 설정이므로 제외)
venv/
env/
.venv/

# --------------------------------------------------------------------
# 3. 대용량 파일 및 모델 제외 (안전 장치)
# --------------------------------------------------------------------
# 혹시 data 폴더 밖에 꺼내놓은 모델 파일이 있을 경우를 대비해 확장자로도 차단
*.onnx
*.pt
*.pth
*.ckpt
*.h5

# --------------------------------------------------------------------
# 4. 기타
# --------------------------------------------------------------------
# Jupyter Notebook 체크포인트
.ipynb_checkpoints